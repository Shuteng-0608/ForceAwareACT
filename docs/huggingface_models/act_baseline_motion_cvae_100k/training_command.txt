# Reconstructed from the preserved experiment configuration and current implementation.
# Verify against the original console log before claiming exact bitwise reproducibility.
# Optional: activate the Python environment that contains this repository's dependencies.
# Example: conda activate forceact

cd /path/to/ForceAwareACT

OUT="outputs/peg_hole_100/act_baseline_motion_cvae_betam5e4_trajectory100k"
mkdir -p "$OUT"

PYTHONPATH=src python scripts/train_act_baseline.py \
  --episode-list outputs/peg_hole_100/all100.txt \
  --normalization-stats outputs/peg_hole_100/normalization_stats_action_all100.pt \
  --action-mode action \
  --chunk-len 10 \
  --image-size 224 224 \
  --camera-names ee_cam base_top_cam \
  --beta-motion-max 5e-4 \
  --warmup-steps 2000 \
  --max-steps 100000 \
  --save-every 10000 \
  --batch-size 16 \
  --num-workers 0 \
  --learning-rate 1e-4 \
  --d-model 128 \
  --z-dim 16 \
  --nhead 4 \
  --num-encoder-layers 1 \
  --num-decoder-layers 1 \
  --dim-feedforward 256 \
  --dropout 0.0 \
  --device cuda \
  --output-dir "$OUT" \
  --log-csv "$OUT/train_log.csv" \
  2>&1 | tee "$OUT/console.log"
